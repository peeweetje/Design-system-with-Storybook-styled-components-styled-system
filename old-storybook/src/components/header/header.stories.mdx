import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Header } from './header.tsx';

<Meta title='Components/Header' component={Header} />

# Header

This is the `header` we are using in our project.

export const Template = (args) => <Header {...args} />;

<Canvas>
  <Story
    name='header'
    args={{ left: 'left', middle: 'middle', right: 'right' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

These are the props were using.

- You have 3 props to place elements in the header.
- The props are optional.
- This is an easy way to place elements in the header quickly.

<ArgsTable of={Header} />
