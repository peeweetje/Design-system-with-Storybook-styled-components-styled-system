import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Button } from "../../src/components/Button.tsx";

<Meta title="Design System|Button" component={Button} />

# Buttons

These are the `Buttons` where are using in our project.

<Preview>
  <Story name="primary">
    <Button status="primary">primary</Button>
  </Story>
  <Story name="secondary">
    <Button status="secondary">secondary</Button>
  </Story>
  <Story name="disabled">
    <Button status="disabled">disabled</Button>
  </Story>
</Preview>

<Props of={Button} />
