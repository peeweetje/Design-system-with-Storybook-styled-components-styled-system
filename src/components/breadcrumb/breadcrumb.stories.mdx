import ReactDOM from "react-dom";
import { NavLink } from "react-router-dom";
import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { BreadCrumb, BreadcrumbPortal } from "./BreadCrumb.tsx";

<Meta title="Components|BreadCrumb" component={BreadCrumb} />

# Buttons

These are the `Breadcrumbs` we are using in our project.

<Preview>
    <Story name="breadcrumbportal"><BreadcrumbPortal/></Story>
  <Story name="BreadCrumb1">
    <Breadcrumb onClick={action('click on link')} to="/one">Breadcrumb1</Breadcrumb>
  </Story>
  <Story name="BreadCrumb2">
    <Breadcrumb onClick={action('click on link')} to="/two">Breadcrumb2</Breadcrumb>
  </Story>
  <Story name="BreadCrumb3">
    <Breadcrumb onClick={action('click on link')} to="/two">Breadcrumb3</Breadcrumb>
  </Story>
</Preview>

## Props

These are the props were using.

<Props of={BreadCrumb} />
