import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Modal } from "./Modal.tsx";

<Meta title="Components|Modal" component={Modal} />

# Modal

These are the `Modals` we are using in our project.

<Preview>
  <Story name="modal">
    <Modal>modal</Modal>
  </Story>
</Preview>

## Props

These are the props were using.

<Props of={Modal} />
