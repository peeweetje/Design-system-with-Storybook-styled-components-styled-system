import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Modal } from "./Modal.tsx";

<Meta title="Components|Modal" component={Modal} />

# Modal

These are the `Modal` we are using in our project.

<React.Fragment>
  <Preview>
    <Story name="modal">
      <Modal>modal</Modal>
    </Story>
  </Preview>
  <Props of={Modal} />
</React.Fragment>
