import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button } from './Button.tsx';

<Meta
  title='Components/Button'
  component={Button}
  argTypes={{
    onClick: { action: 'clicked' },
  }}
/>

# Buttons

These are the `Buttons` we are using in our project.

export const Template = (args) => <Button {...args} />;

<Canvas>
  <Story name='primary' args={{ children: 'primary', variant: 'primary' }}>
    {Template.bind({})}
  </Story>
  <Story
    name='secondary'
    args={{ children: 'secondary', variant: 'secondary' }}
  >
    {Template.bind({})}
  </Story>
  <Story name='disabled' args={{ children: 'disabled', variant: 'disabled' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

These are the props were using.

<ArgsTable of={Button} />
