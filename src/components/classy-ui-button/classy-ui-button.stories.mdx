import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { ClassyButton, baseButton } from "./classy-ui-button.tsx";
import { c, hover, disabled } from "classy-ui/macro";

<Meta title="Components|Classy-ui-Button" component={ClassyButton} />

# Buttons

These are the `Buttons` we are using in our project.

<Preview>
  <Story name="base">
    <Button className={baseButton}>classy-ui</Button>
  </Story>
</Preview>

## Props

These are the props were using.

<Props of={ClassyButton} />
