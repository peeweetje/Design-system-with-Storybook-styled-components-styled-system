import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs";
import { ButtonStyledSystem } from "./ButtonStyledSystem.tsx";

<Meta title="Components|ButtonStyledSystem" component={ButtonStyledSystem} />

# Buttons

These are an example of `Buttons` which uses styled-system styling.

<Preview>
  <Story name="primary">
    <ButtonStyledSystem variant="primary">primary</ButtonStyledSystem>
  </Story>
  <Story name="secondary">
    <ButtonStyledSystem variant="secondary">secondary</ButtonStyledSystem>
  </Story>
  <Story name="disabled">
    <ButtonStyledSystem disabled={boolean("Disabled", true)}>disabled</ButtonStyledSystem>
  </Story>
</Preview>

## Props

These are the props were using.

<Props of={ButtonStyledSystem} />


